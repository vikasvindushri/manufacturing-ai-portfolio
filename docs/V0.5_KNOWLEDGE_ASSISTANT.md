# v0.5 Knowledge Assistant

Version 0.5 provides conversational, evidence-backed answers over local manufacturing documents. Users may upload supported documents, attach metadata, filter scope, ask follow-up questions, inspect citations and diagnostics, and run a frozen retrieval evaluation. Gemini embeddings and synthesis are optional and never replace local fallback.
