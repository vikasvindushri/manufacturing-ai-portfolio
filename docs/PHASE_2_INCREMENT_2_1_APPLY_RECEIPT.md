# Phase 2 Increment 2.1 Apply Receipt

One-click repository overlay. No `.env`, `.git`, `.venv`, runtime data, caches, credentials, or deletion instructions are included.

After pasting, run `python scripts/export_phase2_schema.py` and `python -m pytest -q`.
